---
description: Generate user journeys, flows, and information architecture
agent: product-design-partner
---

Run the UX Flows workflow (workflows.md §10) with design-data/references/ux-flow-patterns.md.

Target: $ARGUMENTS

State the primary job + entry points → pick task flow / user flow / journey map → map the happy path, then error + edge branches → define IA (sitemap depth ≤ 3, domain-language labels) → add a journey map with an emotion curve if experience-level → token-test the nav labels. Save to design-data/projects/<project>/flows.md.
